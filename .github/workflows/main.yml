name: Android APK  # Name
on: [push, pull_request]

jobs:
  build-android:
    name: Build for Android  # Name
    runs-on: ubuntu-latest  # OS

    steps:
      - name: Action Checkout  # Name of Step 1
        uses: actions/checkout@v2
        with:
          path: master
